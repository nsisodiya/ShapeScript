# DeepSeek API key used by the server to power the in-app AI assistant.
# Get one at https://platform.deepseek.com/ — keep it secret, never commit the real value.
DEEPSEEK_API_KEY=

# Port the Express server listens on (local dev). Cloud Run injects PORT automatically.
PORT=3001
